---
- name: Install python module
  pip:
    name: "{{ item }}"
  with_items:
    - boto3

- name: Copy BGP neighbours finder
  copy:
    src=find_bgp_neighbors.py
    dest=/tmp/find_bgp_neighbors.py

- name: Execute BGP neighbours finder
  command: python /tmp/find_bgp_neighbors.py